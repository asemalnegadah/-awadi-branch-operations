name: Apply Zod update

on:
  push:
    branches:
      - chore/deps-zod-4-4
    paths:
      - .github/workflows/apply-zod-update.yml

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: true
          show-progress: false
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json
      - run: npm install --package-lock-only --ignore-scripts --no-audit --no-fund --save-exact zod@4.4.3
      - name: Commit generated manifest and lockfile
        run: |
          set -euo pipefail
          rm .github/workflows/apply-zod-update.yml
          git config user.name "awadi-dependency-bot"
          git config user.email "awadi-dependency-bot@users.noreply.github.com"
          git add package.json package-lock.json .github/workflows/apply-zod-update.yml
          git commit -m "build(deps): update Zod to 4.4.3"
          git push origin HEAD:chore/deps-zod-4-4
